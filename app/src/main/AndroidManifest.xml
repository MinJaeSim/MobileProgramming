<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.yellow7918.mobile_sns">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".UI.UserInfoActivity">
        </activity>

        <activity android:name=".UI.MainActivity">

        </activity>

        <activity android:name=".UI.ArticleAddActivity">

        </activity>

        <activity android:name=".UI.EnrollActivity">

        </activity>

        <activity android:name=".UI.MyPageActivity">

        </activity>

        <activity android:name=".UI.SearchActivity">

        </activity>

    </application>

</manifest>